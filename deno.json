{
  "nodeModulesDir": "auto",
  "lock": {
    "path": "./deno.lock"
  },
  "tasks": {
    "dev": "vite",
    "dev:service": "deno run --watch --allow-net --allow-env --allow-read=.okf-data --allow-write=.okf-data server/main.ts",
    "build": "vite build",
    "start": "deno run --allow-net --allow-env --allow-read=.okf-data,dist --allow-write=.okf-data server/main.ts",
    "typecheck": "tsc --noEmit",
    "lint": "deno lint src server tests vite.config.ts",
    "test": "deno task typecheck && deno task build && deno task lint && deno test --allow-net --allow-env --allow-read --allow-write tests server"
  }
}
